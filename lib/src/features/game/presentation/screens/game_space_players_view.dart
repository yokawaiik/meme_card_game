import 'package:flutter/material.dart';

class GameSpacePlayersView extends StatelessWidget {
  const GameSpacePlayersView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
