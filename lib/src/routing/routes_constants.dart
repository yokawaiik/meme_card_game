const auth = 'Auth';
const authPath = '/auth';

const splash = 'Splash';
const splashPath = '/splash';

const home = 'Home';
const homePath = '/';

const error = 'Error';
const errorPath = '/error';
